# 2026-07-23 工作日志

## 机器码生成逻辑调整（src/utils/machine.py）

### 改动原因
用户反馈担心换网络导致机器码变化（之前机器码包含 MAC 地址 `uuid.getnode()`）。

### 具体修改
1. **移除 MAC 地址参与机器码计算**：删除 `_get_mac_address()` 函数，`_compute_machine_code()` 现在只用磁盘序列号 + CPU ID + 主机名三项，避免网络/网卡变化影响机器码。
2. **机器码持久化到 proxy_db.db**：
   - 新增 `_load_cached_machine_code()`：从 `ProxyDatabase.get_settings()` 读取 `settings.machine_code` 字段
   - 新增 `_persist_machine_code(code)`：通过 `ProxyDatabase.update_settings({"machine_code": code})` 保存
   - `get_machine_code()` 流程改为：内存缓存 → proxy_db.db 读取 → 重新计算并保存
3. **延迟导入**：`_load_cached_machine_code` 和 `_persist_machine_code` 用 `from ..modules.proxy_server import ProxyDatabase` 延迟导入，避免循环依赖。

### 关键设计点
- 机器码一经写入 proxy_db.db 就稳定不变，即便后续硬件信息读取顺序变化、网卡切换、换网络都不影响
- 已有用户的旧机器码（首次启动时基于含 MAC 的算法生成）会在 proxy_db.db 中被新计算的结果覆盖（因为 settings 里没有 machine_code 字段时会重新计算并保存）
- proxy_db.db 仍是 XXTEA 加密的 JSON，机器码作为 settings 子字段存储

## 移除 API Key 重新生成按钮（src/ui/pages/dashboard.py）

### 改动
- 删除仪表盘上 "🔄 重新生成" 按钮（btn_regen_subkey，原 467-484 行）
- 删除对应的 `_regen_subkey()` 方法
- 更新空状态的占位文案：`"sk-（点击重新生成创建）"` → `"sk-（启动服务后自动生成）"`

### 影响
- API Key 现在只在启动服务且无 Key 时自动生成（保留 `_build_config_json` 里的自动生成逻辑）
- 用户不再能手动重置 API Key

## 403 风控触发 BuddyKey 刷新（src/modules/proxy_server.py）

### 改动
在 403/11140 风控处理分支（约 3248 行）增加 BuddyKey 刷新逻辑，与 429/14018 额度耗尽的处理方式对称：

```python
if resp.status_code == 403 and '"code":11140' in resp_body:
    # ... 原有日志 + mark_key_abnormal ...
    # 新增：尝试获取新 BuddyKey 刷新本地 Key
    new_key = self._refresh_buddykey()
    if new_key:
        tried_key_ids.clear()  # 清除已尝试的 key，让重试循环用新 key
        continue
    # 失败则继续换下一个 Key
```

### 行为
- 上游返回 `{"code":11140,"msg":"request illegal"}` 时，先标记当前 Key 为 abnormal，再向服务端请求新 BuddyKey 替换本地 Key，然后用新 Key 重试请求
- 若获取新 BuddyKey 失败，回退到原有故障转移逻辑（换下一个 Key）
- 复用已有的 `_refresh_buddykey()` 方法（2555 行），该方法会：删除旧的 BuddyKey 上游 key → 添加新的 → 刷新路由器缓存

## BuddyKey 刷新次数限制（src/modules/proxy_server.py）

### 改动
在重试循环外新增计数器 `buddykey_refresh_count`（429/403 共享），上限 `MAX_BUDDYKEY_REFRESH = 3`。

- 429 额度耗尽分支和 403 风控分支在调用 `_refresh_buddykey()` 前都检查计数
- 达到 3 次上限时返回 502 + 提示："多次获取 BuddyKey 仍失败，请检查本地是否开启了代理。开启代理会导致上游地址无法被解析。"
- 计数器在循环外初始化，整个请求生命周期内累加，429 和 403 共享同一个计数

## 持久化文件后缀 .db → .key（src/modules/proxy_server.py, src/utils/machine.py）

### 改动
1. `ProxyDatabase.__init__`：`proxy_db.db` → `proxy_db.key`
2. 新增 `_migrate_legacy_db(data_dir)` 方法：首次启动时检测旧 `proxy_db.db`，存在则复制内容到 `proxy_db.key` 并删除旧文件；若新文件已存在则直接删除旧文件
3. `_load` 方法中所有日志文案 `proxy_db.db` → `proxy_db.key`
4. `machine.py` 中所有注释和日志文案同步更新
5. 第 56 行注释 `proxy_db.db` → `proxy_db.key`

### 迁移逻辑
```python
def _migrate_legacy_db(self, data_dir):
    legacy_path = os.path.join(data_dir, "proxy_db.db")
    if not os.path.exists(legacy_path): return
    if os.path.exists(self._db_path):
        os.remove(legacy_path)  # 新文件已存在，直接删旧
        return
    shutil.copy2(legacy_path, self._db_path)  # 复制
    os.remove(legacy_path)  # 删除旧文件
```

### 行为
- 老用户升级后首次启动：`proxy_db.db` 内容复制到 `proxy_db.key`，旧文件删除
- 新用户：直接使用 `proxy_db.key`
- 迁移在 `__init__` 中 `_load()` 之前执行，确保后续读写都使用新文件

## 修复 Linux 解密失败问题（src/modules/proxy_server.py）

### 问题
Linux 用户首次运行 CLI 时报 `proxy_db.key 解密失败，已重置: 解密后长度字段异常: orig_len=2697074788, data_len=356`。

### 原因
1. 旧的 `proxy_db.db` 文件内容是有效 base64 但不是用当前 XXTEA 密钥加密的（可能是旧版本格式或损坏）
2. 迁移逻辑直接复制损坏的旧文件到 `proxy_db.key`，导致 `_load` 解密失败
3. `_load` 删除 `proxy_db.key` 但不删除 `proxy_db.db`，下次启动又会重复迁移

### 修复
1. **迁移前验证解密**：`_migrate_legacy_db` 读取旧文件后先尝试 `_decrypt_json(content)`，验证失败则直接删除旧文件不迁移
2. **空文件直接删除**：旧文件为空时直接删除不迁移
3. **迁移失败也删除旧文件**：异常时也尝试删除旧文件，避免下次重复尝试
4. **日志优化**：解密失败的 WARNING 加上"首次运行或文件损坏属正常现象"提示

### 行为
- 旧文件无法解密 → 直接删除，不迁移 → `_load` 返回空数据 → 后续正常写入新格式
- 再次运行不会重复报错

## 安全评估报告审阅（docs/ANTI_REVERSE_REPORT.md）

### 报告概述
该报告针对 BuddyTool v1.1.5 的逆向分析，列出 7 大弱点并给出分阶段加固路线图。

### 当前代码确认的弱点（仍然存在）
1. **密钥硬编码**：`src/utils/server_api.py` 中 `_AES_KEY_HEX`、`_HMAC_KEY`、`_API_KEY` 全部明文硬编码
2. **SERVER_BASE 明文**：`https://buddy.shengdingit.com/api` 明文存在
3. **_XXTEA_KEY 硬编码**：`proxy_server.py` 第 58 行 `b"AT-XTEA-2026"`
4. **SQLite Schema 明文**：`store.py` 中 `accounts`、`api_keys` 表字段名暴露敏感含义
5. **打包无加固**：`build.bat` 使用 Nuitka onefile，无 `--no-docstrings`，无壳保护
6. **无反调试/反VM/完整性校验**

### 评估结论
报告质量高，分析准确。阶段 1（快速止血）性价比最高，建议优先实施。

## 阶段 1.1：--no-docstrings（build.bat, .github/workflows/build.yml）

### 改动
- `build.bat` 第 32 行新增 `--no-docstrings`
- `.github/workflows/build.yml` 全部 4 处 Nuitka 构建命令新增 `--no-docstrings`
  - Windows 构建、macOS x86_64 构建、macOS ARM64 构建、Linux CLI 构建

## 阶段 1.3：字符串加密（src/utils/_obfuscate.py, server_api.py, proxy_server.py）

### 新增文件
- `src/utils/_obfuscate.py`：字符串混淆工具模块
  - `_derive_key(data_len, salt)`：从数据长度和盐值派生单字节 XOR key
  - `_xor(data, key)`：XOR 加密/解密（对称）
  - `_reveal_str(obfuscated, salt)` / `_reveal_bytes(obfuscated, salt)`：运行时还原
  - `get(name)` / `get_bytes(name)`：从预计算的 `_OBF_DATA` 字典获取还原后的值
  - 每个字符串使用不同的 salt，增加分析难度

### 加密的敏感字符串
| 名称 | 类型 | 原值 |
|------|------|------|
| SERVER_BASE | str | `https://buddy.shengdingit.com/api` |
| AES_KEY_HEX | str | `38502350408f8d5011606fc186daa626196beac6a529d7b79b30e713a0c6f2f0` |
| API_KEY | str | `buddy_707d23cb0832fb0f0fc4a3d7` |
| HMAC_KEY | bytes | `db1a906d80eb73a82d3ded42ffb64be5` |
| XXTEA_KEY | bytes | `AT-XTEA-2026` |

### 修改的文件
- `src/utils/server_api.py`：`SERVER_BASE`、`_AES_KEY`、`_API_KEY`、`_HMAC_KEY` 改为从 `_obfuscate` 模块获取
- `src/modules/proxy_server.py`：`_XXTEA_KEY` 改为从 `_obfuscate` 模块获取

### 效果
编译后的二进制中不再包含明文密钥/URL，`strings` 命令只能看到 XOR 后的乱码字节。

## 阶段 1.4：弃用 XXTEA → AES-256-GCM + 机器绑定（src/modules/proxy_server.py）

### 改动
1. **删除 XXTEA 加密代码**：`_xxtea_encrypt_bytes` 函数删除（`_xxtea_decrypt_bytes` 和 `_xxtea_mx` 保留，仅用于旧格式回退解密）
2. **新增机器绑定密钥派生**：`_get_local_db_key()` — 从磁盘序列号+CPU ID+主机名通过 SHA-256 派生 32 字节 AES 密钥，缓存到 `_local_db_key_cache`
3. **重写 `_encrypt_json`**：使用 AES-256-GCM，格式为 `base64( magic(4B "BTG1") + nonce(12B) + ciphertext+tag )`
4. **重写 `_decrypt_json`**：支持三种格式自动识别：
   - 新格式（magic `BTG1` 开头）→ AES-256-GCM + 机器绑定密钥
   - 旧格式（无 magic，前 4 字节是 orig_len）→ XXTEA + 旧硬编码密钥回退
   - 明文 JSON → 直接解析
5. **文件头标记** `_DB_MAGIC_V2 = b"BTG1"` 区分新旧格式

### 关键设计
- **旧文件自动迁移**：老用户的 `proxy_db.key` 用 XXTEA 加密，新代码能读取并解密；下次保存时自动用新格式写入
- **机器绑定**：换机器/重装系统后旧 `proxy_db.key` 无法解密（密钥不同），会触发 `_load` 的容错逻辑删除文件并重置
- `secrets.token_bytes(12)` 生成 nonce（替代 `os.urandom`，因为 proxy_server.py 顶部未导入 os）
- `hashlib` 已在文件顶部导入，直接使用

### 测试验证
- 新格式加密 → 解密：通过
- 空数据加密 → 解密：通过
- 明文 JSON 兼容：通过
- 旧 XXTEA 格式回退解密：通过

## 阶段 2.4：证书固定 Certificate Pinning

### 新增文件
- `src/utils/ssl_pinning.py`：证书固定模块
  - `_verify_spki(cert_der)`：验证证书 SPKI SHA-256 指纹
  - `_PinnedHTTPSConnection`：自定义 HTTPS 连接，握手后验证 SPKI
  - `_PinnedPoolManager`：替换连接池的连接类
  - `PinnedHTTPAdapter`：requests 适配器
  - `install_pinning(session)`：为 Session 安装 pinning

### 修改文件
- `src/utils/_obfuscate.py`：新增 `SERVER_SPKI_HASH` 加密数据
- `src/utils/server_api.py`：
  - 创建全局 `_session = requests.Session()`
  - `_session.trust_env = False` 禁用系统代理
  - `install_pinning(_session)` 安装证书固定
  - 所有 `requests.post(...)` 改为 `_session.post(...)`
  - 移除 `proxies=_NO_PROXY` 参数（trust_env=False 已覆盖）
  - 移除 `import requests`（改为 `import requests as _requests_module`）

### 关键设计
- **SPKI 指纹而非证书指纹**：服务端用 Let's Encrypt 证书（90天轮换），但公钥通常不变，pin SPKI 更持久
- **SPKI hash**：`16c28b6c981e422990835db06024f6e369f6f2c803642c45348e1cb4bb24c2ba`（SHA-256）
- **仅 pinned 域名**：只对 `buddy.shengdingit.com` 启用，其他域名正常验证
- 指纹通过 XOR 混淆存储，不暴露在二进制中

### 效果
- Fiddler/Charles 等抓包工具即使安装了自签名 CA，也会因 SPKI 不匹配被拒绝连接
- 与字符串加密配合，形成"字符串看不到 + 流量抓不到"的双重防护

### 验证
- 模块导入正常
- 实际 HTTPS 请求通过 pinning 验证（返回 401 是因为缺签名头，TLS 连接成功）
