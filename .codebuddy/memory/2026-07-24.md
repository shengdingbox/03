# 2026-07-24 工作日志

## Nuitka 加固 + UPX + 字符串混淆扩展

### 1. UPX 压缩（build.bat, .github/workflows/build.yml）

#### build.bat
- 新增 UPX 检测逻辑：`where upx` 检查是否安装
- 找到 UPX 时设置 `--upx-dir="."` 参数启用压缩
- 未找到 UPX 时打印警告，跳过压缩（不阻断构建）

#### build.yml（4 个平台）
| 平台 | UPX 安装方式 | --upx-dir 参数 |
|------|-------------|---------------|
| Windows | `crazy-max/ghaction-upx@v3` | `--upx-dir="."` |
| macOS Intel | `brew install upx` | `--upx-dir="$(brew --prefix upx)/bin"` |
| macOS ARM | `brew install upx` | `--upx-dir="$(brew --prefix upx)/bin"` |
| Linux CLI | `apt-get install upx-ucl` | `--upx-dir="/usr/bin"` |

### 2. 字符串混淆扩展（api_client.py 敏感 URL）

#### 新增加密的字符串
| 名称 | 原值 | salt |
|------|------|------|
| BILLING_API_BASE | `https://copilot.tencent.com` | 0x51 |
| PUBLIC_API_BASE | `https://codebuddy.cn` | 0x37 |
| KEYCLOAK_TOKEN_URL | `https://www.codebuddy.cn/auth/realms/copilot/protocol/openid-connect/token` | 0x84 |
| CHAT_COMPLETIONS_URL | `https://copilot.tencent.com/v2/chat/completions` | 0x4A |

#### 修改文件
- `src/utils/_obfuscate.py`：`_OBF_DATA` 字典新增 4 个 URL 的加密数据
- `src/modules/api_client.py`：
  - `BILLING_API_BASE`、`PUBLIC_API_BASE`、`KEYCLOAK_TOKEN_URL` 改为 `_obf_get()` 获取
  - `check_api_key_chat_status()` 中的硬编码 URL 改为 `_obf_get("CHAT_COMPLETIONS_URL")`

### 效果
- UPX 压缩减小可执行文件体积 + 增加静态逆向难度
- `api_client.py` 中的 4 个敏感 URL 不再以明文存在于二进制中
- `strings` 命令无法直接提取 copilot.tencent.com / codebuddy.cn 等地址

## 修复 macOS Dock 不显示问题（src/main.py）

### 问题
macOS 上 Nuitka onefile 生成的是纯可执行文件（非 `.app` bundle），默认不会出现在 Dock 和应用切换器 (Cmd+Tab) 中，用户关闭窗口后无法找到应用。

### 修复
在 `QApplication` 创建后添加 macOS Dock 显示逻辑：
1. 尝试用 `AppKit.NSApp.setActivationPolicy_(1)` 设置为普通应用（Regular）
2. `AppKit` 不可用时回退到纯 ctypes 方式调用 `libobjc.dylib`
3. `setActivationPolicy_(1)` = NSApplicationActivationPolicyRegular，让程序出现在 Dock
4. `activateIgnoringOtherApps_(True)` 强制激活窗口

### macOS 最低版本改为 13（.github/workflows/build.yml）
- 两个 macOS 构建任务 runner 从 `macos-14` 改为 `macos-13`
- 新增环境变量 `MACOSX_DEPLOYMENT_TARGET: "13"`
- 与 PySide6 6.11.1 wheel 的 `macosx_13_0` 要求一致

## 兼容 macOS 12 方案（暂存，未实施）

### 背景
当前最低支持 macOS 13，因 PySide6 6.11.1 wheel 为 `macosx_13_0_universal2`。
用户希望兼容 macOS 12 (Monterey)。

### 关键发现
- **PySide6 6.8.3** 是最后支持 macOS 12 的版本（`macosx_12_0_universal2`）
- 发布日期：2025年3月27日
- Python 要求：3.9+
- 项目代码未使用 PySide6 6.9+ 新 API，6.8.3 完全兼容

### 待实施方案
| 项目 | 当前 | 改为 |
|------|------|------|
| PySide6 | 6.11.1 | **6.8.3** |
| CI runner | macos-15-intel / macos-15 | **macos-13** |
| MACOSX_DEPLOYMENT_TARGET | "13" | **"12"** |
| Python | 3.13 | **3.11** |

### 改动范围（待实施时）
1. `requirements.txt` — PySide6 6.11.1 → 6.8.3
2. `pyproject.toml` — Python 版本要求
3. `build.yml` — runner 改回 macos-13，MACOSX_DEPLOYMENT_TARGET 改 "12"，Python 改 3.11
4. `build.bat` — 本地 venv 的 PySide6 降级

### 风险
- `macos-13` runner 之前卡住过（GitHub 可能淘汰）
- 备选：用 `macos-14` runner + `MACOSX_DEPLOYMENT_TARGET=12`，编译产物仍兼容 macOS 12

### 实施记录（2026-07-25 已实施）
1. `requirements.txt` — PySide6 6.11.1 → 6.8.3（含 Addons / Essentials）
2. `pyproject.toml` — `requires-python` 从 `>=3.13` 改为 `>=3.11`，PySide6 版本同步降级
3. `.github/workflows/build.yml`：
   - Intel 构建：`macos-15-intel` → `macos-13`，`MACOSX_DEPLOYMENT_TARGET` 从 `"13"` 改为 `"12"`
   - ARM 构建：`macos-15` → `macos-14`（macos-13 是 Intel runner），`MACOSX_DEPLOYMENT_TARGET` 从 `"13"` 改为 `"12"`
4. `build.bat` — 无需修改（不涉及版本号）
5. Python 版本已是 3.11，无需改动

### 最低兼容性
| 组件 | 最低 macOS |
|------|-----------|
| PySide6 6.8.3 | 12.0 (Monterey) |
| cryptography 48.0.0 | 10.9 |
| Python 3.11 | 10.15 |
| **综合** | **macOS 12.0** |
